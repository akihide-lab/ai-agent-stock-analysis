# 株の銘柄分析プロジェクト運用ルール

## 初動の入口制御

このプロジェクトでは、ユーザーから株式、銘柄、投資判断、おすすめ銘柄、企業分析、株価、IR、決算に関する依頼を受けた場合、回答やWeb検索を始める前に、必ずローカルのプロジェクト文脈を確認する。

最初に確認する対象は次の通り。

- `README.md`
- `logs/feedback_improvements.md`
- `scripts/`
- 既存の株式分析、質問判定、銘柄解決、レポート生成に関係するファイル

## 回答前の禁止事項

ローカル確認を行う前に、次の行動をしない。

- 一般的な投資アドバイスとして回答する
- 銘柄候補を提示する
- ユーザー発話を独自に細かく切り分けて処理方針を確定する
- Web検索ベースで相場環境や個別銘柄を説明する

## 優先する処理順序

株式関連の依頼では、次の順序を守る。

1. 依頼が株式関連かを粗く判定する
2. 株式関連であれば、まずローカルのREADME、ログ、関連スクリプトを確認する
3. 既存の入口フローやルーターがある場合は、それを優先して使う
4. ローカルフローで不足する最新株価、IR、決算、ニュースが必要な場合だけWeb検索を行う
5. 回答では、ローカル分析結果と外部最新情報を分けて示す

## レポート作成までの必須ゲート

株式関連依頼では、AIの判断で途中の質問、入口フロー、DB更新、レポート生成を省略しない。

特に `question_agent.py` などの入口フローが次の状態を返した場合は、そこで停止する。

- `route=followup_required`
- `classification_status=insufficient`
- `intent_status=insufficient`

この場合、候補提示、仮条件設定、レポート生成、Web補足へ進まない。
入口フローが返した不足項目と質問文をユーザーに提示し、ユーザーの回答を受けてから同じ入口フローへ再投入する。

`followup_required` は参考情報ではなく、停止ゲートとして扱う。

ユーザーが明示的に「仮で進めて」と指示した場合だけ、仮条件を置いて先へ進めてもよい。

銘柄または検索条件が確定した後は、次の順序で進める。

1. 既存の入口フロー、銘柄解決、候補選定を実行する
2. 対象銘柄が決まったら、既存のレポート生成フローを実行する
3. ローカルDBにデータが不足する場合は、Web回答より先にDB更新スクリプトの実行可否を確認する
4. DB更新後、再度レポート生成フローを実行する
5. それでも不足する情報だけ、公式IR、決算、ニュース、株価などを外部補足として確認する
6. 最終回答では、ローカルフロー結果、外部補足、未実行または失敗理由を分けて示す

AIの推論は、主に次の2点に限定する。

- 入力側: あいまいな発話を Intent、Entity、Missing Info、対象銘柄候補へ整理し、既存フローへ渡す
- 出力側: フロー実行後のログ、分析結果、レポート、補足情報をユーザー向けに整理する

フロー途中では、README、ログ、既存スクリプトに従い、自己判断で順序を飛ばさない。

## Web検索の扱い

金融・投資関連では最新情報が重要だが、このプロジェクトではWeb検索は初手ではなく補助手段として扱う。

Web検索は、ローカルの分析フローで対象銘柄、分析目的、不足情報が明確になった後に行う。

## フィードバックの記録

ユーザーから進め方、期待との差分、品質、確認不足についてフィードバックを受けた場合は、再利用できる内容か判断し、必要に応じて `logs/feedback_improvements.md` に構造化Markdownで追記する。
